import styled from 'styled-components';

export const FirstTitle = styled.div`
  h1 {
    font-size: 4em;
    line-height: 1;
  }
  p {
    max-width: 550px;
  }
  a {
    padding: 10px;
    background: #8491a0;
    color: #fff;
    position: relative;
    top: 14px;
  }
  a:hover {
    background: var(--yellow);
    color: var(--black);
  }
`;
